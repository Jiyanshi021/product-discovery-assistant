# product-discovery-assistant
End-to-end system that scrapes fashion products, stores them in a structured database, generates embeddings for semantic search, and recommends relevant products based on abstract user queries.
